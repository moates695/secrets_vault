# Fail2ban filter for Vaultwarden failed /admin panel logins.
# Matches lines like:
#   [2026-01-01 12:00:00.000][vaultwarden::api::admin][ERROR] Invalid admin token. IP: 1.2.3.4.
[Definition]
failregex = ^.*Invalid admin token\. IP: <ADDR>.*$
ignoreregex =
datepattern = ^\[%%Y-%%m-%%d %%H:%%M:%%S
